# Read Server CPU and Memory Usage

## Mini docs

- usage-cpu-mem.js is the module that computes the servers cpu and memory usage.

- To view example, run the following commands:
  - `npm install`
  - `node app.js`
  